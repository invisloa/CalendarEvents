<?xml version = "1.0" encoding = "UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:CalendarT1"
             x:Class="CalendarT1.App">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles/Colors.xaml" />
                <ResourceDictionary Source="Resources/Styles/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <!-- Define the style for labels -->
            <Style TargetType="Label">
                <Setter Property="FontFamily" Value="MyNormalFont" />
                <Setter Property="FontSize" Value="16" />
                
            </Style>

            <!-- Define the style for buttons -->
            <Style TargetType="Button">
                <Setter Property="FontFamily" Value="MyNormalFont" />
                <Setter Property="MinimumHeightRequest" Value="40" />
                <Setter Property="FontSize" Value="16" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="TextColor" Value="{DynamicResource MainTextColor}"/>
                <Setter Property="VisualStateManager.VisualStateGroups">
                    <VisualStateGroupList>
                        <VisualStateGroup x:Name="CommonStates">
                            <VisualState x:Name="Normal">
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="{DynamicResource MainButtonBackgroundColor}" />
                                </VisualState.Setters>
                            </VisualState>
                            <VisualState x:Name="Disabled">
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="LightGray" />
                                    <!-- or any other color you prefer -->
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateGroupList>
                </Setter>
            </Style>

            <Color x:Key="MainTextColor">#fff</Color>
            <Color x:Key="MainBackgroundColor">#3b78d4</Color>
            <Color x:Key="MainButtonBackgroundColor">#104fff</Color>
        </ResourceDictionary>
    </Application.Resources>
</Application>
